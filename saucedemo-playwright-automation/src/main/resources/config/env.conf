# Environment and Browser Configuration
conf {
  # Target URL for automation
  url = "https://www.saucedemo.com/"

  # Browser to be used: CHROMIUM, FIREFOX or WEBKIT
  # Note: CHROMIUM covers Chrome and Edge. WEBKIT covers Safari.
  browser = "CHROMIUM"

  # Global timeout for actions and page loading (in milliseconds)
  # 10000 ms = 10 seconds
  timeout = 10000

  # Run browser without GUI (true for CI/CD pipelines)
  headless = false
}

# User Credentials (to be used by Login Page)
user {
  standard {
    username = "standard_user"
    password = "secret_sauce"
  }
  locked {
    username = "locked_out_user"
    password = "secret_sauce"
  }
}